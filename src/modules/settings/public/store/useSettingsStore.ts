/** Focused public entrypoint; importing this contract evaluates only its owner. */
export type { CatalogSortMode } from '../../store/useSettingsStore';
export type { CatalogType } from '../../store/useSettingsStore';
export type { HomeSectionId } from '../../store/useSettingsStore';
export { useSettingsStore } from '../../store/useSettingsStore';
